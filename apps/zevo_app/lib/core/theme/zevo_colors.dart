import 'package:flutter/material.dart';

/// Central color palette for the ZEVO application.
class ZevoColors {
  /// Background color: #0D0D0D
  static const Color background = Color(0xFF0D0D0D);

  /// Primary card / surface color: #171717
  static const Color surface = Color(0xFF171717);

  /// Primary royal purple accent color: #7C5CFF
  static const Color accent = Color(0xFF7C5CFF);

  /// Primary text color (White): #FFFFFF
  static const Color textPrimary = Color(0xFFFFFFFF);

  /// Secondary text color (Grey): #9CA3AF
  static const Color textSecondary = Color(0xFF9CA3AF);

  /// Border color: #262626
  static const Color border = Color(0xFF262626);

  /// Success color (Green): #22C55E
  static const Color success = Color(0xFF22C55E);

  /// Gold color for Champion achievements: #F5C542
  static const Color championGold = Color(0xFFF5C542);
}
